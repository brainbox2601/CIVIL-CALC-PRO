services:
  - type: web
    name: civil-calc-pro-api
    env: node
    buildCommand: npm install
    startCommand: npm start  # This triggers "node src/server.js" from package.json
    envVars:
      - key: NODE_ENV
        value: production

